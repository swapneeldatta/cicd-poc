name: buga-cicdtest
version: '1.0.0'
summary: CI-CD with snap
description:  A test to check out CICD with snaps
grade: stable
confinement: strict

apps:
  hello:
    command: bin/hello
  java:
    command: java -jar /root/workspace/parts/gnu-hello/build/target/CDTest-0.0.1-SNAPSHOT.jar

parts:
  gnu-hello:
    plugin: maven
    source: https://github.com/swapneeldatta/cicd-poc.git
    stage-packages: [ openjdk-8-jre, ca-certificates-java, maven]

